@model FacturadorListadoViewModel

@{
    ViewData["Title"] = "Administración de facturadores";

    var puedeCrear =
        User.TienePermiso(PermisosSistema.Facturadores.Crear);

    var puedeEditar =
        User.TienePermiso(PermisosSistema.Facturadores.Editar);

    var puedeCambiarEstado =
        User.TienePermiso(PermisosSistema.Facturadores.Inactivar);
}

<div class="d-flex flex-wrap justify-content-between
            align-items-center gap-3 mb-4">
    <div>
        <h1 class="h3 mb-1">Administración de facturadores</h1>
        <p class="text-muted mb-0">
            Gestione los responsables disponibles para facturación.
        </p>
    </div>

    @if (puedeCrear)
    {
        <a asp-action="Crear" class="btn btn-primary">
            Crear facturador
        </a>
    }
</div>

@if (ViewData["MensajeExito"] is string mensajeExito)
{
    <div class="alert alert-success" role="alert">
        @mensajeExito
    </div>
}

@if (ViewData["MensajeError"] is string mensajeError)
{
    <div class="alert alert-danger" role="alert">
        @mensajeError
    </div>
}

<div class="card shadow-sm border-0">
    <div class="card-header bg-white">
        <strong>Facturadores registrados</strong>
        <span class="badge text-bg-secondary ms-2">
            @Model.Facturadores.Count
        </span>
    </div>

    <div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
            <thead class="table-light">
                <tr>
                    <th>Código</th>
                    <th>Nombre</th>
                    <th>Estado</th>
                    <th>Auditoría</th>
                    <th class="text-end">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Facturadores.Count == 0)
                {
                    <tr>
                        <td colspan="5"
                            class="text-center text-muted py-4">
                            No hay facturadores registrados.
                        </td>
                    </tr>
                }
                else
                {
                    @foreach (var facturador in Model.Facturadores)
                    {
                        <tr>
                            <td>
                                <strong>@facturador.Codigo</strong>
                            </td>
                            <td>@facturador.Nombre</td>
                            <td>
                                @if (facturador.Activo)
                                {
                                    <span class="badge text-bg-success">
                                        Activo
                                    </span>
                                }
                                else
                                {
                                    <span class="badge text-bg-secondary">
                                        Inactivo
                                    </span>
                                }
                            </td>
                            <td class="small">
                                @if (facturador.FechaModificacionUtc.HasValue)
                                {
                                    <span>
                                        Modificado por
                                        <strong>@facturador.ModificadoPor</strong>
                                    </span>
                                    <br />
                                    <span class="text-muted">
                                        @facturador.FechaModificacionUtc.Value.ToString("yyyy-MM-dd HH:mm 'UTC'")
                                    </span>
                                }
                                else
                                {
                                    <span>
                                        Creado por
                                        <strong>@facturador.CreadoPor</strong>
                                    </span>
                                    <br />
                                    <span class="text-muted">
                                        @facturador.FechaCreacionUtc.ToString("yyyy-MM-dd HH:mm 'UTC'")
                                    </span>
                                }
                            </td>
                            <td>
                                <div class="d-flex flex-wrap
                                            justify-content-end gap-1">
                                    @if (puedeEditar)
                                    {
                                        <a asp-action="Editar"
                                           asp-route-codigo="@facturador.Codigo"
                                           class="btn btn-sm
                                                  btn-outline-primary">
                                            Editar
                                        </a>
                                    }

                                    @if (puedeCambiarEstado)
                                    {
                                        @if (facturador.Activo)
                                        {
                                            <form asp-action="Inactivar"
                                                  asp-route-codigo="@facturador.Codigo"
                                                  method="post"
                                                  class="d-inline">
                                                <button type="submit"
                                                        class="btn btn-sm
                                                        btn-outline-danger">
                                                    Inactivar
                                                </button>
                                            </form>
                                        }
                                        else
                                        {
                                            <form asp-action="Activar"
                                                  asp-route-codigo="@facturador.Codigo"
                                                  method="post"
                                                  class="d-inline">
                                                <button type="submit"
                                                        class="btn btn-sm
                                                        btn-outline-success">
                                                    Activar
                                                </button>
                                            </form>
                                        }
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
