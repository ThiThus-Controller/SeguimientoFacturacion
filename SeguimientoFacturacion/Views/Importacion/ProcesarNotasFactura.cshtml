@model SeguimientoFacturacion.ViewModels.Importacion.ProcesamientoLoteNotasFacturaViewModel

@{
    ViewData["Title"] = "Procesar notas";
}

<div class="row justify-content-center">
    <div class="col-12 col-lg-8">
        <div class="card shadow-sm border-danger">
            <div class="card-header bg-danger text-white">
                <h1 class="h4 mb-0">
                    Procesamiento definitivo de notas
                </h1>
            </div>

            <div class="card-body">
                <div asp-validation-summary="ModelOnly"
                     class="alert alert-danger"
                     role="alert">
                </div>

                <p>
                    Esta operación trasladará las notas crédito
                    y débito desde staging hacia las tablas
                    definitivas de facturación.
                </p>

                <p>
                    Antes de guardar se verificarán nuevamente
                    las facturas relacionadas, sus aseguradoras,
                    las fechas y las notas ya existentes.
                </p>

                <dl class="row">
                    <dt class="col-sm-5">
                        Identificador del lote
                    </dt>
                    <dd class="col-sm-7">
                        <code>@Model.LoteId</code>
                    </dd>
                </dl>

                <div class="alert alert-warning" role="alert">
                    <strong>Verificación requerida:</strong>
                    confirme que existe una copia de seguridad
                    reciente de la base de datos.
                </div>

                <form asp-controller="Importacion"
                      asp-action="ProcesarNotasFactura"
                      method="post"
                      onsubmit="return confirm('Esta operación importará definitivamente las notas crédito y débito. ¿Desea continuar?');">
                    @Html.AntiForgeryToken()

                    <input asp-for="LoteId" type="hidden" />

                    <button type="submit"
                            class="btn btn-danger">
                        Procesar notas definitivamente
                    </button>

                    <a asp-controller="Importacion"
                       asp-action="Index"
                       class="btn btn-outline-secondary ms-2">
                        Cancelar
                    </a>
                </form>
            </div>
        </div>
    </div>
</div>
